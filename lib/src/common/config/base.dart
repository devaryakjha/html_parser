import 'package:flutter/widgets.dart';

/// Base class for all configuration classes
@immutable
abstract class HtmlConfigBase {
  /// Default constructor
  const HtmlConfigBase();
}

/// Configuration class for the HTML parser
@immutable
final class HtmlConfig extends HtmlConfigBase {
  /// Default constructor
  const HtmlConfig();
}
